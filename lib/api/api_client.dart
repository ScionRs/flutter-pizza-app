

import 'dart:convert';
import 'dart:io';
import 'package:http/http.dart' as http;
import 'package:pizza_app/Data/pizza_data.dart';

class ApiClient {
  

  /* List<PizzaData> products = [];
   http.Response response = await http.get(Uri.parse('http://localhost:3000/pizza'));
   if(response.statusCode == 200) {
     print(response.statusCode);
     print(response.body);
     String jsonString = response.body;
     List data = jsonDecode(jsonString);
     products = data.cast<PizzaData>();
   }
*/
    /*
    List<PizzaData> products = [];
    http.Response response = await http.get(Uri.parse('http://localhost:3000/pizza'));
    if(response.statusCode == 200) {
      print(response.statusCode);
      print(response.body);
      String jsonString = response.body;
      List data = jsonDecode(jsonString);
      products = data.map((e) => PizzaData.fromJson(e)).toList();
    }
     */
  }
